import type { Viewport } from "next";

export const viewport: Viewport = {
	width: "device-width",
	colorScheme: "dark",
	themeColor: "#09090b",
	initialScale: 1,
	maximumScale: 5,
	minimumScale: 1,
	userScalable: true,
};
